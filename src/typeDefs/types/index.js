const { postType } = require("./postType");
const { userType } = require("./userType");
const { addressType } = require("./addressType");
const { companyType } = require("./companyType");
const { geoLocationType } = require("./geoLocationType");

module.exports = {
  postType,
  userType,
  addressType,
  companyType,
  geoLocationType,
};